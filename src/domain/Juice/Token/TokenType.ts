export enum TokenType {
    LEFT_PARENTHESIS,
    RIGHT_PARENTHESIS,
    LEFT_BRACKET,
    RIGHT_BRACKET,
    MINUS,
    PLUS,
    SEMICOLON,
    SLASH,
    STAR,
    MODULO,
    HASH,
    DOT,

    BANG,
    BANG_EQUAL,
    EQUAL,
    EQUAL_EQUAL,
    GREATER,
    GREATER_EQUAL,
    LESS,
    LESS_EQUAL,

    IDENTIFIER,
    UINT_TYPE,
    STRING_TYPE,
    BOOLEAN_TYPE,

    STRING,
    NUMBER,
    BOOLEAN,

    OBJECT,
    NEW,
    ELSE,
    FUNCTION,
    FOR,
    IF,
    NULL,
    RETURN,
    VAL,
    WHILE,
    AND,
    OR,

    EOF,
}
